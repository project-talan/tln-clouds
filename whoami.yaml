apiVersion: v1
kind: Namespace
metadata:
  name: whoami
---
apiVersion: v1
kind: Service
metadata:
  name: whoami
  namespace: whoami
spec:
  selector:
    component: whoami
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whoami
  namespace: whoami
spec:
  replicas: 1
  selector:
    matchLabels:
      component: whoami
  template:
    metadata:
      labels:
        component: whoami
      annotations:
        rollme: "{{ randAlphaNum 5 | quote }}"
    spec:
      restartPolicy: Always
      containers:
        - name: whoami
          image: traefik/whoami
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-whoami
  namespace: whoami
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/$2"
    nginx.ingress.kubernetes.io/path-regex: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: "${TLN_CLOUDS_WHOAMI_HOST}"
      http:
        paths:
          - path: "/whoami(/|$)(.*)"
            pathType: "ImplementationSpecific"
            backend:
              service:
                name: whoami
                port:
                  number: 80